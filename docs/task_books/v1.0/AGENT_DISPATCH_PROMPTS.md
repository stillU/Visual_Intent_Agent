# v1 Agent 派发指令

将下列指令交给后续实施 Agent。默认单 Agent 顺序处理，不自行创建新任务或扩展功能。

~~~text
请执行 docs/task_books/v1.0/README.md，先读取 00_v0_6_static_review.md、适用 AGENTS.md 和实际工作区内容，保护用户与其他 Agent 的未提交成果。

用户要求快速整理 v1，不运行测试或验证。目标仅为最小候选交付：建议统一为 1.0.0rc1，同步元数据/包版本/CLI 展示及 uv.lock 的本项目版本项，不升级依赖；提供不含凭据的配置模板；精简 README，操作细节移入使用指南；交付候选版本说明与交接。

沿用现有源码 CLI、SQLite、确认/PIN、反馈与本地 RAG，不改领域合同/数据库 Schema/默认 RAG 行为。不修改 v0.5 已批准语料、原审核决定或 v0.6 冻结配置。v0.6 real 入口仍不可实际执行，真实评测/盲评未完成，不能只改布尔值绕过门禁。COCO-CN 许可仍待定。

本次只静态阅读与必要编辑：不要执行 pytest、校验脚本、Fake 演练、CLI 启动、安装/构建、哈希复算或真实服务；不要读取 .env/api.md、覆盖凭据或修改用户数据库。历史测试数字必须标为前序报告，不能当本轮结果。

交付 docs/releases/v1.0.0rc1.md 和 docs/handoffs/v1_0_handoff.md，说明修改文件、未改合同、使用入口、未执行项与遗留风险；同步任务索引。只能报告“候选交付整理完成，未测试/未验证”，不能宣称已验证稳定或 RAG 更优。

不自动提交、推送、打 tag 或 stash。若发现必须扩大运行时修改范围才能处理的问题，先记录证据并请求用户决定，不为赶版本擅自重构。
~~~
