# v0.6 静态检查记录

日期：2026-09-17。方法：只读交接、配置与代码；未运行测试、校验工具、Fake 演练或真实请求。本文不是运行验收结果。

## 已看到的交付

- [Step 01 报告](../../handoffs/v0_6_step_01_handoff.md)：协议、30 个候选上下文、标注、配置与冻结清单。
- [Step 02 报告](../../handoffs/v0_6_step_02_handoff.md)：B/C 对照上下文、记录、预算、执行器、续跑及相关测试文件。
- evaluation/v0_6/ 中存在 context.py、records.py、budget.py、paired_runner.py、reporting.py 等实现；对照上下文使用固定 revision ID，两个 arm 使用独立数据库。
- [启动待决定清单](../../handoffs/v0_6_launch_request.md)明确保留预算、模型、阈值及评审安排等待定项。

以上只说明材料和实现存在；报告中的通过数字是前序 Agent 的记录，本轮没有复核其运行正确性或全面审查全部异常路径。

## 必须保留的结论

| 项目 | 静态状态 | 对 v1 的影响 |
|---|---|---|
| v0.6 授权 | run_config.json 中 real_run_authorized=false，预算与模型身份未确认 | 不得启动真实评测 |
| 真实执行入口 | paired_runner.py 的 real 模式显式阻止构造真实 Provider | 不是“只等填 key”的完整执行器，不应宣传正式评测可用 |
| 盲评/质量结果 | 本次看到 Step 01/02，未见 Step 03/04 的已完成交付 | 不宣称 RAG 收益或 v0.6 完整完成 |
| 应用版本 | pyproject.toml 与包版本仍 0.1.0，CLI 展示仍为 v0.4 工程预览 | v1 整理时统一当前版本展示 |
| 使用文档 | 根 README 含大量操作步骤，当前状态混合多个历史版本 | 将细节移到使用指南，README 突出目标与架构 |
| 知识快照 | 沿用 v0.5 五条 approved 的既有记录 | 不重写语料、审批或哈希 |
| 数据许可 | COCO-CN 仍待确认 | 保持隔离，不修改 v0.5 部分完成结论 |
| 工作区 | 大量既有修改与未跟踪交付文件 | 保留成果，不自动 commit/stash 或只用旧 HEAD 宣称已发布 |

## 本次裁定

可以将 v1 定位为现有产品的最小候选交付整理，不必继续扩展评测才能编写任务书。但暂停测试/评测不能转换成“已经通过”：v0.6 状态仍是离线准备交付，v1 本轮不具有新增运行验证证据。

本次没有实施版本升级、启动改造或配置修改。先前拟议的实现补丁未应用成功；随后只编写本任务目录与索引。
