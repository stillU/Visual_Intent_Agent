# Agent 派发指令

以下可直接交给实施 Agent，默认单 Agent 顺序执行，不授权自行创建其他任务、正式评测或提交推送。

~~~text
请执行 docs/task_books/post_v0.5_fixes/README.md 和 ACCEPTANCE.md。

先读项目规则、v0.5 原任务与交接、审核记录及当前实现，保护已有未提交成果。按 F1→F2→F3→集中验收最小收尾，不重做 RAG。

F1：核验并原字节归档五份获批 v2 draft 提案到 docs/knowledge_reviews/v0.5-approved-1/proposals/，附归档清单和历史路径映射。测试和验证工具改读可交付归档，继续验证原批准哈希及仅审核三字段变化。不修改知识库 v0.4/v0.5 JSONL/manifest，不改审核决定。缺失或哈希不符时停止该项并报告，不能反向重造或 skip。

F2：将 Step 01 旧派生物明确归为审核前历史准备，不再计为当前已执行验收。新建绑定 v0.5-approved-1 和 manifest 哈希的当前清单，三路径各十场景，映射确切测试并执行。复用已有测试，只补缺口；成功场景用最终生产语料和临时真实数据库。负例隔离被测原因，不用 draft 过滤掩盖条件/模型/冲突分支。不复制许可待定文本或保留池原文，不虚称原创工程用例属于官方 benchmark。

F3：COCO-CN 许可仍暂不确认，保持隔离与部分完成状态；未经用户明确决定不缩减范围或启动新的使用。该项不阻断独立的 F1/F2 收尾。

完成局部和集中全量离线回归，并在不含 data/.env/用户数据库的隔离副本验证可复现性。副本含当前未提交新文件，不只复制旧 HEAD；不移动真实 data。确认导入来自副本。

交付 docs/handoffs/post_v0_5_fixes_001_handoff.md，列出证据、哈希、三十场景结果、两环境回归、未决许可和版本状态。同步当前入口，历史报告保留原结果并链接补充报告。

不变更已批准知识/领域合同/PIN/确认/候选，不扩建数据库或 RAG，不用真实 Provider、不评测、不读真实凭据；未经要求不提交推送或开始下一版本。需要扩大运行时修改时先报告并请求裁定。
~~~
