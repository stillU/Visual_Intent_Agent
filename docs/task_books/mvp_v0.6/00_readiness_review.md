# v0.5 收尾状态复查

复查日期：2026-09-16。依据 [收尾策略](../post_v0.5_fixes/README.md) 和 [实施交接](../../handoffs/post_v0_5_fixes_001_handoff.md)。本次复查未修改运行时代码或知识内容。

## 实际检查结果

| 项目 | 本次独立结果 |
|---|---|
| 原工作区全量离线回归 | uv run pytest -q：2665 passed、3 deselected，退出码 0 |
| 提案归档 | 验证工具默认读取 docs/knowledge_reviews/v0.5-approved-1/；五份提案及归档清单通过 |
| 当前验收清单 | --check 核验三十个 case 的结构及测试节点通过；节点执行包含在上述全量回归，不把 --check 本身视为已执行 |
| 无暂存隔离副本 | /tmp/v05-review-m9WcFN；复制当前代码、测试、evaluation、tools、knowledge_base、docs 与配置，不含 data、.env、用户数据库或 .git |
| 隔离提案验证 | 五份提案及归档清单通过，导入路径确认来自副本 |
| 隔离全量回归 | 2664 passed、1 skipped、3 deselected，退出码 0；skip 仅因副本无 Git 元数据，无法测试忽略规则 |
| 快照保护 | 冻结字节守卫及发布相对提案仅改变审核三字段的守卫通过 |
| 正式服务与图片评测 | 未调用、未执行；默认排除三项 smoke |

结论：F1/F2 的已知收尾问题通过复核，审核材料已可移交，三十场景为当前可执行清单。没有发现阻断 v0.6 离线准备的新增问题。本检查聚焦收尾与回归，不等于外部模型质量或全部系统安全审计。

## 仍需明确的事项

1. COCO-CN 归档精确许可仍待确认，v0.5 继续部分完成。不能靠 v0.6 新计划修改这一历史状态。
2. 当前 HEAD 为 b039b9b7cd49becd0a25e251b37d930fee07da88，工作区 dirty，许多交付文件尚未提交。正式运行沿用 evaluation/identity.py 的真实 commit + clean 门禁，不能只写 HEAD 掩盖未提交实现。
3. 项目 Provider 别名 qwen-image-3.0 尚未在本次复查核实对应模型。真实运行前须核对实际服务文档和返回身份；无法追溯版本时停止正式批次，不虚构映射。
4. 五条规则是已批准的项目默认偏好，不是外部物理定律或已证实的画质提升；保留词法检索与三路径范围限制。

临时副本只是本次验证证据，不是后续任务依赖。后续 Agent 必须从项目内已归档输入重建环境；不要引用该临时路径作为长期数据源。
