# v0.6 G1 启动确认请求（初版）

- 日期：2026-09-16
- 状态：**用户已决定 v0.6 不启动真实评测；全部收益、图片质量与正式验收延后至 v1**。
  当前及本版保持 `real_run_authorized = false`；不得调用真实服务、执行正式批次或产出质量结论。
- 依据：[Step 01 任务书](../task_books/mvp_v0.6/01_protocol_and_cases.md) §「G1 启动确认
  清单」、[v0.6 Step 01 交接](v0_6_step_01_handoff.md)、
  [v0.6 Step 02 交接](v0_6_step_02_handoff.md)、
  [v0.6 协议](../../evaluation/v0_6/protocol.md)、
  [v0.5 收尾状态复查](../task_books/mvp_v0.6/00_readiness_review.md)。
- 提醒：笼统“继续”**不构成**预算与批次授权；以下第 1～5 项需要具体回答。

## 0. 本轮请求的一句话

Step 01～02 离线准备已完成：协议、30 个预注册候选上下文（18/6/6）、标注、运行配置、
冻结清单与最小 B/C 执行器均已落盘，并通过 Fake 链路演练和离线回归。用户已决定本版不做
收益、图片质量或正式验收；以下第 1～5 项保留为 v1 启动时的输入清单，不构成当前待执行授权。

## 1. 精确代码 / 协议 / 语料身份

| 项目 | 当前值 | 需要用户决定 |
|---|---|---|
| 代码 commit | `b039b9b7cd49becd0a25e251b37d930fee07da88`（`b039b9b`，分支 `feature`） | 是否需要先冻结为 clean commit？谁来提交？ |
| 工作区 | **dirty 且未提交**（本批新增路径均未跟踪；具体 status 项数随工作区变化） | 是否接受 dirty 状态，或安排提交后另起新 run？ |
| 协议 | `evaluation/v0_6/protocol.md`，`bc_protocol_v0_6`，sha256 `70100d6f…` | 是否冻结当前字节？ |
| 配置 | `evaluation/v0_6/run_config.json`，`bc_v0_6`，sha256 `f9adae7d…` | 预算/阈值/模型字段是否按第 4 项填写？ |
| 候选集 | `evaluation/v0_6/cases.jsonl`，`bc_cases_v0_6`，sha256 `b5a9df73…` | 是否接受这 30 个上下文？是否需独立人工预审？ |
| 标注 | `evaluation/v0_6/annotations.jsonl`，sha256 `02f6a750…` | 是否接受预注册期望与分层？ |
| 冻结清单 | `evaluation/v0_6/frozen_manifest.json`，sha256 `9514acb0…` | 是否以此清单作为正式门禁？ |
| 知识语料 | `knowledge_base/v0.5/`，`v0.5-approved-1`，5 条 approved，fingerprint `c961790c…`；manifest `0bbeea9b…` | 是否确认沿用该快照？ |
| 检索/分词/渲染 | `lexical.v1` / `keyword.v1` / 既有 `QwenImageRenderer` | 是否确认版本不再变？ |

- 冻结清单角色路径为仓库根相对路径，解析须 `load_manifest(path, base_dir=<repo root>)`。
- 当前工作区 dirty：正式运行沿用 `evaluation/identity.py` 的真实 commit + clean 门禁；
  不能只写 HEAD 掩盖未提交实现；**Agent 不会为变 clean 自动 commit/stash**。

## 2. Provider、模型身份与真实参数

| 项目 | 当前值 | 需要用户确认 |
|---|---|---|
| Provider base_url / key | 仅环境变量引用，值不入交付物 | 实际服务与凭据由谁提供？在何处核对？ |
| LLM 模型 | `VIA_LLM_MODEL`，**身份未核实** | 精确模型 ID 与版本；如何追溯返回身份？ |
| 图片模型 | `qwen-image-3.0`（**项目别名，未核实**） | 官方版本 ID；无法追溯则停止正式批次 |
| 图片尺寸 | `1024x1024`，每请求 1 张 | 是否确认？ |
| 超时 / 重试 `r` | 未确认（`null`） | 超时秒数、最大重试 `r`、退避策略 |
| seed | 语料 `target_models=["any"]`；图片 seed 策略为“仅记录不伪造” | Provider 是否支持显式 seed？是否冻结 B/C 相同 seed 对？ |
| 适配器参数 | 不擅自新增参数 | 是否确认不设置 temperature/max_tokens 等 |

- 记录中不写明文密钥；API endpoint 需去除敏感查询参数。

## 3. 请求量与尝试上限

- 基础：`30 上下文 × 2 次独立生成重复 × B/C 两侧 = 120` 次基础图片请求。
- 上限：`120 × (1 + r)` 次底层尝试（`r` 为单次最大重试次数）；**费用不明的超时也计入
  已消耗尝试**；失败补跑/试运行额外计入，不能暗含免费调用。
- 若需要连通性小试：单列范围与预算，输出不得当正式样本，也不得用于改阈值。
- 请确认：`r = ?`；请求上限 `= ?`；尝试上限 `= ?`；是否允许连通性小试（范围/预算）？

## 4. 精确金额上限、截止条件、评审人与阈值

| 项目 | 当前值 | 需要用户填写 |
|---|---|---|
| 金额上限 | `null` | 精确金额与币种；单价来源 |
| 请求/尝试上限 | `null` | 见第 3 项 |
| 截止条件 | 安全违例 / 身份或哈希漂移 / 预算耗尽 / 未知费用 | 是否补充其它停止条件？ |
| 评审人 | 未指定 | 姓名/身份；工作量（对数 × 时间）；分歧处理 |
| 主指标阈值 | `null` | 最小有意义收益（净胜率） |
| 完成率差值 | `null` | 允许的 B/C 完成率差值 |
| 耗时上限 | `null` | 端到端 / 检索耗时上限 |
| 成本上限 | `null` | 与金额上限一致或单列 |
| 分层报告 | `L1_applicable` / `L2_explicit_pin_control` / `L3_no_hit_fallback` | 是否接受分层与聚类区间口径？ |

- 阈值必须在**看结果之前**确认；未确认不输出达标判定。

## 5. 是否执行真实批次

- [x] **v0.6 不授权真实批次**：仅保留已完成的离线 Fake 链路演练；不验证收益和图片质量。
- [ ] **授权正式批次**：本版不适用；如 v1 需要，须基于届时的新冻结配置重新授权。
- [ ] **仅授权连通性小试**：本版不适用；已执行的是零网络 Fake 链路，不是真实连通性调用。

用户决定：**所有收益、图片质量和正式验收统一延后至 v1**。v0.6 不再等待 G1 真实运行授权。

## 6. 尚需用户裁定的既有事项（与本次授权正交）

1. **v0.5 COCO-CN 归档精确许可**仍待确认；v0.5 继续部分完成，本版不改变该状态，
   COCO-CN 不进入本候选集。
2. **独立人工预审**：30 个候选样本与标注由本 Agent 预注册，尚未经独立审核人预审；
   请指定审核人（或确认接受探索性证据定位）。
3. 如需冻结代码身份：请安排提交；Agent 不自动 commit/stash。

## 7. 后续安排

1. Step 02 已完成：Fake 环境下的最小 B/C 执行器已验证配对、公平性、预算与续跑。
2. v0.6 保持 `real_run_authorized=false`，不执行原计划 Step 03/04。
3. v1 如需真实运行、盲评、收益或图片质量验收，应重新核验并冻结代码、模型、预算、样本、阈值与评审安排；不得把本文件视为 v1 的自动授权。
